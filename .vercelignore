# Vercel은 api/ 아래 파일을 하나씩 서버리스 함수로 만든다.
# 테스트 파일도 예외가 아니라서 /api/outfit/deliver.test 같은 함수가 6개 생기고 있었다.
# 배포 로그에 이 파일들이 컴파일되는 게 그대로 찍힌다. 런타임에는 쓸모가 없다.
*.test.ts
*.test.tsx

# 점검용 스크립트. 로컬에서 .env를 읽어 Supabase를 들여다보는 용도라 배포본에 갈 이유가 없다.
scripts/

# Supabase에 직접 실행하는 DDL. 저장소에도 올리지 않기로 했다 (PLAN.MD 3.3).
schema/
