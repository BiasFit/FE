# BiasFit FE React 전환 원칙

## 목표

`BiasFit_full_service_feedback_applied.html`의 사용자·인플루언서 전체 흐름과 시각 구성을 React로 거의 동일하게 재현한다. 화면만 복사하지 않고 Style DNA 계산, TOP 3 매칭, 2인 그룹 입력 분리, 부탁해요 카드, 코디 카드 임시저장·전달·다운로드가 실제 상태로 연결되는 동작형 MVP를 만든다.

## 작업 범위

- 모든 파일 생성·수정·설치·빌드는 `FE` 폴더 안에서만 수행한다.
- 루트의 원본 HTML과 제품 문서는 읽기 전용 기준으로 사용한다.
- 실제 개인정보, 실제 계정, 실제 사용자 사진은 사용하지 않는다.
- 결제, 커머스, 소셜 기능, 실시간 채팅 등 PRD 제외 범위는 추가하지 않는다.

## 구조

```text
FE/
├─ public/assets/          원본 HTML의 화면 이미지
├─ src/
│  ├─ app/                 HashRouter, 전역 상태, 화면 전환
│  ├─ features/user/       사용자 진단·매칭·요청·결과 화면
│  ├─ features/influencer/ 프로필·배정 요청·코디 작성 화면
│  ├─ domain/              Style DNA, 그룹 조합도, TOP 3 계산
│  ├─ data/                P1~P5와 인플루언서 더미 데이터
│  ├─ shared/              Shell, 버튼, 카드, 입력, 모달, Toast
│  ├─ storage/             프로필 및 코디 초안 localStorage 처리
│  └─ styles/              토큰, 공통 컴포넌트, 화면, 반응형 CSS
└─ ARCHITECTURE.md
```

## 구현 원칙

- 기존 해시 경로, 문구, CSS 클래스와 화면 순서를 최대한 유지한다.
- `App.tsx`는 라우터와 Provider 조립만 담당한다.
- 전역 흐름은 `Context + useReducer`, 입력폼은 controlled component로 관리한다.
- 그룹 구성원 A와 B의 입력은 별도 객체에 저장해 서로 덮어쓰지 않는다.
- 점수 계산은 UI와 분리된 순수 함수로 만들고 P1~P5 고정값으로 검증한다.
- 인플루언서 초안 키는 `influencerId + requestId`로 분리하고 변경 약 1초 뒤 자동 저장한다.
- 원본 Base64 이미지는 정적 에셋으로 분리하고, 코디 카드 이미지는 npm `html2canvas`로 다운로드한다.
- 원본과의 시각 차이는 393×852 모바일과 데스크톱 화면에서 확인한다.
- 오류나 뒤로가기 후에도 입력값과 작성 중인 내용은 유지한다.

## 구현 순서

1. 테스트·라우팅·이미지 다운로드 의존성과 공통 타입을 설정한다.
2. Style DNA, 그룹 조합도, 인플루언서 매칭 계산을 테스트 우선으로 구현한다.
3. 앱 상태, 더미 데이터, 해시 라우팅과 공통 Shell을 연결한다.
4. 사용자 15개 화면을 원본 HTML과 동일한 흐름으로 구현한다.
5. 인플루언서 5개 화면과 계정별 초안 자동저장을 구현한다.
6. 원본 CSS와 이미지를 이식해 모바일·데스크톱 시각 동등성을 맞춘다.
7. 코디 카드 다운로드, 오류 상태, 접근성, 반응형을 검증한다.
8. 테스트, 타입 검사, 프로덕션 빌드, 주요 흐름 브라우저 검증을 실행한다.
